# Plan: Fix Dashboard "Dead" Issue

## Root Cause
`dashboard.js` has a JavaScript syntax error: lines 505–510 contain orphaned statements outside any method body (after line 504 already closes `updatePortfolioOverview`). The stray block also references a nonexistent `formatPercentage` method. A JavaScript parser immediately aborts on such errors, so neither `BAETDashboard` is defined nor any script runs — the dashboard appears dead with no data.

## Fix
Remove lines 505–510 (the orphaned duplicate `updatePortfolioOverview` epilogue) from `dashboard.js`.

## Files Changed
- `src/baet/dashboard/web/dashboard.js` — remove lines 505–510 (7 lines)
